services:
  kokoro-tts:
    image: ghcr.io/remsky/kokoro-fastapi-cpu:latest
    container_name: kokoro-local-api
    ports:
      - "8880:8880"
    restart: unless-stopped
    environment:
      - TZ=Europe/Paris
    volumes:
      - kokoro-models:/app/models

volumes:
  kokoro-models: